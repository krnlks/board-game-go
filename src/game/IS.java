package game;

//TODO Turn into class IS with 'state' (E,B,W) and 'location' (center, edge, corner)
public enum IS {   //an intersection that is either empty, with black stone or with white stone
    E, B, W
}//enum IS